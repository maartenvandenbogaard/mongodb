apiVersion: kubedb.com/v1alpha1
kind: MongoDB
metadata:
  name: mgo-rs2
  namespace: demo
spec:
  version: "3.4"
  replicas: 3
  replicaSet:
    name: rs0
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
